input CurrencyInput {
    id: Int
    name: String!
    idcountry: Int
    isCrypto: Boolean
}

type CurrencyDto {
    id: Int!
    name: String
    idcountry: Int
    isCrypto: Boolean
}

type Query {
    currency: [CurrencyDto!]!
    searchcurrency(id:Int): CurrencyDto
}